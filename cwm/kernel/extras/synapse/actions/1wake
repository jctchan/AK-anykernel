reset_uci() {
  /sbin/uci reset;
  /sbin/uci;
}

if [ -f $1 ]; then
	if [[ ! -z $2 ]]; then
		echo $2 > $1
		[ "$1" == "/sys/devices/virtual/misc/touchwake/enabled" ] && echo "0" > /sys/devices/virtual/input/lge_touch/dt_wake_enabled;
		reset_uci;
	fi;	
 echo `cat $1`;
fi;
